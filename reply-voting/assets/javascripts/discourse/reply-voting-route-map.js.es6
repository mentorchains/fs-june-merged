/**
 * Links the path `/reply_voting` to a route named `reply_voting`. Named like this, a
 * route with the same name needs to be created in the `routes` directory.
 */
 export default function () {
    this.route('reply_voting', { path: '/reply_voting' });
  }